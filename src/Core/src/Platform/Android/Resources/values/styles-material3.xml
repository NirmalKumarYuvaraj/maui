<?xml version="1.0" encoding="UTF-8"?>
<resources>
    <!-- Material 3 Base Theme (Automatically switches between light/dark) -->
    <style name="Maui.Material3.Theme.Base" parent="Theme.Material3.DayNight">
        <!-- For .NET 9 we opt out of edge to edge enforcement by default -->
        <item name="maui_edgetoedge_optout">true</item>
        <item name="maui_splash">false</item>
        <item name="bottomNavigationViewStyle">@style/Widget.Material3.BottomNavigationView</item>
        <item name="materialButtonStyle">@style/MauiMaterialButton</item>
        <item name="mauiShellTabBarStyle">@style/MauiShellTabBar.Material3</item>
        <item name="mauiShellToolbarStyle">@style/MauiShellToolbar.Material3</item>
        <item name="mauiShellFlyoutStyle">@style/MauiShellFlyout.Material3</item>
        <!-- Default color tokens used by ShellRenderer when no ShellAppearance
             override is supplied. Resolved at runtime via Context.Theme so
             dark mode is handled by Theme.Material3.DayNight. -->
        <item name="mauiShellBarBackgroundColor">?attr/colorSurface</item>
        <item name="mauiShellBarForegroundColor">?attr/colorOnSurface</item>
        <item name="mauiShellBarTitleColor">?attr/colorOnSurface</item>
        <item name="mauiShellBarUnselectedColor">?attr/colorOnSurfaceVariant</item>
        <item name="mauiShellTabBarBackgroundColor">?attr/colorSurface</item>
    </style>


    <!-- Material 3 No Action Bar Theme -->
    <style name="Maui.Material3.Theme.NoActionBar" parent="Maui.Material3.Theme.Base">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="android:actionModeBackground">?attr/colorSurface</item>
        <item name="android:actionModeStyle">@style/Material3ActionMode</item>
    </style>

    <!-- Material 3 Action Mode -->
    <style name="Material3ActionMode">
      <item name="android:background">?attr/colorSurface</item>
      <item name="android:height">?attr/actionBarSize</item>
    </style>

    <style name="MauiMaterialButton" parent="Widget.Material3.Button">
      <!-- remove all the min sizes as MAUI manages it -->
      <item name="android:minWidth">0dp</item>
      <item name="android:minHeight">0dp</item>
      <!-- padding is the space between the edge of the background and the contents  -->
      <item name="android:paddingLeft">0dp</item>
      <item name="android:paddingRight">0dp</item>
      <item name="android:paddingTop">0dp</item>
      <item name="android:paddingBottom">0dp</item>
      <!-- insets are the amount of space between the actual color and the edge of the view frame -->
      <item name="android:insetTop">0dp</item>
      <item name="android:insetBottom">0dp</item>
      <item name="android:insetLeft">0dp</item>
      <item name="android:insetRight">0dp</item>
      <!-- Icon size attribute to allow proper icon sizing in Material 3 -->
      <item name="iconSize">0dp</item>
    </style>

    <!-- ===== Shell styles (Material 3) =====
         Mirrors the Material 2 equivalents but resolves colors against
         Material 3 tokens (colorSurface, colorOnSurface, …). These are
         selected automatically when MauiAppCompatActivity applies
         Maui_Material3_Theme_NoActionBar. -->
    <style name="MauiShellTabBar.Material3" parent="Widget.Material3.BottomNavigationView">
      <item name="android:background">?attr/colorSurface</item>
      <item name="itemActiveIndicatorStyle">@style/Widget.Material3.BottomNavigationView.ActiveIndicator</item>
      <item name="itemIconTint">?attr/colorOnSurface</item>
      <item name="itemTextColor">?attr/colorOnSurface</item>
    </style>

    <style name="MauiShellToolbar.Material3" parent="Widget.Material3.Toolbar">
      <item name="android:background">?attr/colorSurface</item>
      <item name="titleTextColor">?attr/colorOnSurface</item>
      <item name="subtitleTextColor">?attr/colorOnSurfaceVariant</item>
    </style>

    <style name="MauiShellFlyout.Material3">
      <item name="android:background">?attr/colorSurface</item>
      <item name="android:textColorPrimary">?attr/colorOnSurface</item>
    </style>
</resources>